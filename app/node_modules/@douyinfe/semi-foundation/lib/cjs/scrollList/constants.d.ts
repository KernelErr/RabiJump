declare const cssClasses: {
    PREFIX: string;
    SELECTED: string;
};
declare const strings: {
    readonly MODE: readonly ["normal", "wheel"];
};
declare const numbers: {
    DEFAULT_ITEM_HEIGHT: number;
    DEFAULT_SCROLL_DURATION: number;
};
export { cssClasses, strings, numbers };
