declare class Store<T = Record<string, any>> {
    _state: T;
    _listeners: any[];
    constructor(initialState: T);
    subscribe(listener: (state: T) => () => void): () => void;
    setState(state: T): void;
    getState(): T;
}
export default Store;
